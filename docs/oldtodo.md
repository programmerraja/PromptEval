
UI
- [] make side pannel collapsible
- [] introduce dark theme
- [] from prompt palyground allow user to add to dataset (where for multi conversation)
      - need prompt to extract dataset from multi conversation where user can edit that prompt
    - for single prompt allow them to add the placeholder value to dataset

single prompt eval 
- where they write prompt and pass input and see how the output is 


Dataset for single prompt
- the placeholders 



Eval page
- Allow user to add different Eval prompts 
- Config the  models 
- To run 
    single prompt
    - choose the prompt + dataset  => send to LLM and get the answer use the EVAL prompt and again send to LLM with llm answer and get the EVAL result show to the customer
   MultiPrompt
    - choose the prompt and dataset and run the simulation allow user to view the conversation stop/pause etc after compeleteion send the conversation to EVAL prompt and get the EVAL result show to the customer


Question 
- how to handle if we have tools?



Setting page
- Allow user to set default models for all usecase globally
- storage management 
    - allow user to export and import the data



Prompt page
- on playground allow user to edit prompt (auto save it)
- when creating prompt ask the user does it single turn or multi turn
- on eval result tab show the prompt previous or stored eval scores etc.



if u have any doubt need clarity before start ask me dont assume it 